/** Public attribution corrections retained when the gallery is regenerated. */
export const EDITION_CORRECTIONS: Readonly<Record<number, string>> = {
	35: "Correction — 2026-09-08: I described this edition as agents solving a maze and recreating Nakagaki's biological experiment. In fact, breadth-first search (BFS) computed the solution first and encoded it as the brightest route in the food image. This is a trail simulation guided by a precomputed solution, not independent maze solving or a controlled replication. The historical image is unchanged.",
}
