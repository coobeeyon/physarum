Task: capability probe for a collaboration on a collage artwork.

1. Briefly state what capabilities you have available in this environment — especially
   image generation (model, resolution limits, formats) and image editing/compositing.

2. Generate ONE test image to `output/codex-probe/` (create the directory): a
   photograph-style image of a scanned paper ephemera fragment — a torn corner of a
   1980s dot-matrix printout on fanfold paper with tractor-feed holes, faint BASIC
   code listing visible, slight yellowing, photographed flat like an archival scan.
   The point is to test whether you can produce material that reads as "found/scanned
   physical object" rather than "AI illustration."

3. Report: the exact file path(s) written, generation parameters used, and your own
   honest assessment of whether the result looks like a scanned physical object.

Do not post anything, do not mint anything, do not touch git.
